# 2026-06-21 - GLM 5.2 default and fallback

Commits shipped:

- `troublemaker` `ca7682dcfc0bd9059a0c5f764148941ddacfd9ea`
- `pi` `ef2b35d72c131c5cd3227025499ac3ee0e6580f4`
- `flight` `d9bc388a5624c7deb370c4575243822d47246862`
- `fat-platform` `274ecdaa81070f08f483c012e1ee9ea280b80be8`
- `crawdad-cf` `aa53e63cc781ab7479195f3cc15ba40fff6a2ddc`

Summary:

- Made Fireworks GLM 5.2 (`accounts/fireworks/models/glm-5p2`) the Troublemaker default.
- Added built-in GLM 5.2 metadata so runtime resolution works even before the installed Pi package includes the model.
- Updated `/model glm`, `/model glm5`, and `/model glm-5.2` aliases to resolve to GLM 5.2 while keeping explicit GLM 5.1 aliases.
- Added model auth fallback so a selected model without configured auth falls back to Fireworks GLM 5.2.
- Removed edge-runtime imports that pulled Pi coding-agent runtime code into the edge bundle.

Verification:

- `npm run typecheck`
- `npm run test:fireworks-model-resolution`
- `npm run test:model-config-current-selection`
- `npm run test:model-config-list-models`
- `npm run test:web-ui-model-autocomplete`
- `npm run test:edge-runtime-import`
- `npm run build`

Deploy status:

- `crawdad-cf` deployed from `tiny-bat` with `--containers-rollout=immediate`.
- Container image confirmed `CRAWDAD_VERSION=369`.
- Cloudflare Worker version: `88688240-d1ee-4f0d-a376-96b8718d24c2`.
- Zip was restarted, configured to `fireworks/accounts/fireworks/models/glm-5p2`, restarted again, and described back as provider `fireworks`, model `accounts/fireworks/models/glm-5p2` at `2026-06-21T05:42:55.964Z`.

Manual QA gaps:

- Did not send a user-facing Zip message after the final restart. The operator describe readback verified durable model selection without creating a chat turn.
- Zip received one web chat during the first post-deploy wake and resolved the old `openai-codex/gpt-5.5` setting before the operator configure completed; subsequent readback confirmed GLM 5.2 is now selected.
